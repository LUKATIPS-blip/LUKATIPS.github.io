function showTime() {
	document.getElementById('currentTime').innerHTML = new Date().toUTCString();
}
showTime();
setInterval(function () {
	showTime();
}, 1000);
body{
  padding: 25px;
}
.title {
	color: #5C6AC4;
}
<!DOCTYPE html>
<html>
  <head>
    <title>Hello, World!</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
      <h1 class="title">BIENVENIDOS! </h1>
      <p id="currentTime"></p>
      <script src="script.js"></script>
  </body>
</html><!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>LUKATIPSPRO | NBA Betting Expert</title>

<link href="https://fonts.googleapis.com/css2?family=Anton&family=Montserrat:wght@400;600&display=swap" rel="stylesheet">

<style>
:root{
  --purple:#552583;
  --yellow:#FDB927;
  --dark:#050505;
  --card:#1b0c2c;
  --white:#ffffff;
}

*{margin:0;padding:0;box-sizing:border-box;font-family:'Montserrat',sans-serif}

body{
  background:radial-gradient(circle at top,#2b134f,#050505 70%);
  color:var(--white);
}

header{
  text-align:center;
  padding:30px 20px;
  background:linear-gradient(145deg,var(--purple),#3a175c);
  border-bottom:4px solid var(--yellow);
}

header h1{
  font-family:'Anton',sans-serif;
  font-size:3rem;
  color:var(--yellow);
}

.container{max-width:1100px;margin:auto;padding:50px 20px}
.section{margin-top:80px}

.section h2{
  font-family:'Anton',sans-serif;
  text-align:center;
  color:var(--yellow);
  margin-bottom:30px;
  letter-spacing:2px;
}

.hero{text-align:center}
.hero h3{
  font-family:'Anton',sans-serif;
  font-size:2rem;
  color:var(--yellow);
  margin-bottom:15px;
}

.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:30px;
}

.card{
  background:linear-gradient(145deg,#3b1b5a,var(--card));
  border-radius:20px;
  padding:30px;
  border:2px solid rgba(253,185,39,.25);
  box-shadow:0 15px 40px rgba(0,0,0,.6);
  cursor:pointer;
  transition:.2s;
}

.card:hover{
  transform:translateY(-4px);
}

.card h4{color:var(--yellow);margin-bottom:10px}

.copy{
  font-size:.85rem;
  opacity:.8;
  margin-top:10px;
}

.btn{
  display:inline-block;
  margin-top:15px;
  padding:12px 18px;
  background:var(--yellow);
  color:#1a1a1a;
  font-weight:600;
  border-radius:10px;
  text-decoration:none;
}

form input,form button{
  width:100%;
  padding:12px;
  margin-top:15px;
  border-radius:8px;
  border:none;
}

form input{
  background:#0b0b0b;
  color:#fff;
  border:1px solid #333;
}

form button{
  background:var(--yellow);
  font-weight:700;
  cursor:pointer;
}

#dashboard{
  display:none;
  background:#020617;
  padding:40px;
  border-radius:20px;
  border:1px solid #1e293b;
}

footer{
  margin-top:100px;
  padding:25px;
  text-align:center;
  font-size:.85rem;
  border-top:3px solid var(--yellow);
  background:#050505;
}

footer strong{color:var(--yellow)}
</style>
</head>

<body>

<header>
  <h1>LUKATIPSPRO</h1>
  <p>Tipster profesional de la NBA · Análisis · Disciplina · Resultados</p>
</header>

<main class="container">

<section class="hero">
  <h3>APUESTAS NBA CON VISIÓN PROFESIONAL</h3>
  <p>Análisis avanzado, gestión de banca y disciplina profesional.</p>
</section>

<!-- PAGOS -->
<section class="section">
<h2>MÉTODOS DE PAGO</h2>

<div class="grid">

<div class="card" onclick="copiar('970 719 266')">
<h4>Yape (Perú)</h4>
<p>Número: 970 719 266</p>
<p>Nombre: Alexis Montes</p>
<div class="copy">Click para copiar</div>
</div>

<div class="card" onclick="copiar('5428785180237942')">
<h4>Mercado Pago</h4>
<p>Cuenta: 5428785180237942</p>
<p>Nombre: Mara Alejaldre</p>
<div class="copy">Click para copiar</div>
</div>

<div class="card" onclick="copiar('lucasaballone13')">
<h4>Argentina</h4>
<p>Alias: lucasaballone13</p>
<div class="copy">Click para copiar</div>
</div>

<div class="card">
<h4>PayPal</h4>
<p>Pago internacional seguro</p>
<a class="btn" href="https://paypal.me/Rushapo?country.x=AR&locale.x=es_XC" target="_blank">
Pagar con PayPal
</a>
</div>

</div>
</section>

<!-- COMPROBANTE -->
<section class="section">
<h2>ENVIAR COMPROBANTE</h2>

<div class="card" style="max-width:600px;margin:auto">
<form onsubmit="activarPanel(event)">
<input type="text" placeholder="Nombre completo" required>
<input type="text" placeholder="Método de pago usado" required>
<input type="file" accept="image/*,.pdf" required>
<button type="submit">Enviar comprobante</button>
</form>

<p style="margin-top:20px;text-align:center">
Telegram:<br>
<a href="https://t.me/LukaTips77" target="_blank" style="color:var(--yellow);text-decoration:none">
@LukaTips77
</a>
</p>
</div>
</section>

<section class="section">
<h2>PANEL PRIVADO</h2>

<div id="dashboard">
<p>Comprobante enviado correctamente.  
Acceso final tras validación manual.</p>
</div>
</section>

<section class="section">
<h2>AVISO LEGAL</h2>
<div class="card">
<p>
LUKATIPSPRO no garantiza resultados económicos.  
Las apuestas implican riesgo.  
Pagos no reembolsables.
</p>
</div>
</section>

</main>

<footer>
© 2025 <strong>LUKATIPSPRO</strong> · NBA Betting Analyst
</footer>

<script>
function copiar(texto){
  navigator.clipboard.writeText(texto);
  alert("Datos copiados al portapapeles");
}

function activarPanel(e){
  e.preventDefault();
  document.getElementById("dashboard").style.display="block";
}
</script>

</body>
</html>
